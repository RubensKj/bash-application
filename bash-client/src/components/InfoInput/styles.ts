import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  justify-content: flex-start;

  user-select: none;

  color: var(--color-info-user) !important;
  font-size: var(--font-size-bash) !important;
`;
